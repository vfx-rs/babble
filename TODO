- [ ] function templates
- [ ] store fwd decls so we can handle them when we encounter them in signiatures but the type is never defined
- [ ] automatically rename const/non-const method pairs
- [ ] handle namespacing classes for lookup (build qualified name on class creation?)